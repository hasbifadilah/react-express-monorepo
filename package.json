{
  "name": "react-express-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client": "yarn workspace @app/client",
    "server": "yarn workspace @app/server",
    "build": "yarn client build && yarn server build",
    "start": "yarn server start",
    "dev": "yarn client start && yarn server dev --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "lerna": "^5.1.1"
  }
}
